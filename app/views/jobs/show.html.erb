<h1> <%= @job.title %> </h1>

<p>
  <%= simple_format(@job.description) %>

  <div class="text-center">
    <%= link_to("Submit a resume", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration:underline;") %>
  </div>

  <p class="lead">
    <%= link_to 'Back', jobs_path, class: 'btn btn-danger' %>
  </p>

</p>
